---
import Layout from '../layouts/main.astro'
import { TemplateManager } from '../components/TemplateManager'

const content = {
  title: 'SendGrid Template Viewer'
};
---

<Layout content={content}>
  <TemplateManager client:load />
</Layout>
